---
name: sync
description: 중앙 볼트의 키를 연결된 모든 프로젝트 .env에 전파한다. 트리거 — "/tikeytaka:sync", "키 동기화해줘", "키 전파해줘", "env 반영해줘", 키 갱신 직후.
---

# tikeytaka:sync — 볼트 → 프로젝트 전파

코어 CLI: `bash "${CLAUDE_PLUGIN_ROOT}/bin/tkt"` (이하 `tkt`)

```bash
tkt sync --check   # 1) 드라이런: 무엇이 바뀔지 먼저 확인
tkt sync           # 2) 실제 전파
```

- 드라이런 결과가 예상 밖으로 많으면(모든 파일이 바뀐다든가) 실행 전에 사용자에게 확인한다 — 볼트 암호 불일치나 매핑 오류 신호일 수 있다.
- 전파 후 변경 건수만 보고한다. 값은 출력하지 않는다.
- 이미 떠 있는 프로세스(서버·데몬)는 재시작해야 새 키를 읽는다는 점을 변경이 있었을 때만 한 줄로 알린다.
